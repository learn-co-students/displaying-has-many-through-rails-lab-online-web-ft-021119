<h1>Doctors Available<h1>

<ul>
    <% @doctors.each do |doctor| %>
        <li>Doctor <%= link_to doctor.name, doctor_path(doctor) %></li>
        <li>Patients: <%= doctor.patients.count %> </li>
    <% end %>
</ul>