<h1>Doctor <%= @doctor.name %> </h1>
<h2>Department <%= @doctor.department %> <h2>

<p>Appointments: <br />
    <% @doctor.appointments.each do |appt|%>
        <%= appt.appointment_datetime.strftime('%B %d, %Y at %H:%M') %>
        with Patient <%= link_to appt.patient.name, patient_path(appt.patient) %>
    <% end %>
</p>